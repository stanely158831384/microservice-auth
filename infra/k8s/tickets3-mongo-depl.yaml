apiVersion: apps/v1
kind: Deployment
metadata:
  name: tickets3-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tickets3-mongo
  template:
    metadata:
      labels:
        app: tickets3-mongo
    spec:
      containers:
        - name: tickets3-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: tickets3-mongo-srv
spec:
  selector:
    app: tickets3-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017

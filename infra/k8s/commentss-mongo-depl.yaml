apiVersion: apps/v1
kind: Deployment
metadata:
  name: commentss-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: commentss-mongo
  template:
    metadata:
      labels:
        app: commentss-mongo
    spec:
      containers:
        - name: commentss-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: commentss-mongo-srv
spec:
  selector:
    app: commentss-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017

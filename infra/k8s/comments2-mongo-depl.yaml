apiVersion: apps/v1
kind: Deployment
metadata:
  name: comments2-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: comments2-mongo
  template:
    metadata:
      labels:
        app: comments2-mongo
    spec:
      containers:
        - name: comments2-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: comments2-mongo-srv
spec:
  selector:
    app: comments2-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017

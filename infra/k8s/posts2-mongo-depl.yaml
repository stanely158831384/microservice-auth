apiVersion: apps/v1
kind: Deployment
metadata:
  name: posts2-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: posts2-mongo
  template:
    metadata:
      labels:
        app: posts2-mongo
    spec:
      containers:
        - name: posts2-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: posts2-mongo-srv
spec:
  selector:
    app: posts2-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017

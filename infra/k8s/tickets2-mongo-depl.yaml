apiVersion: apps/v1
kind: Deployment
metadata:
  name: tickets2-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tickets2-mongo
  template:
    metadata:
      labels:
        app: tickets2-mongo
    spec:
      containers:
        - name: tickets2-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: tickets2-mongo-srv
spec:
  selector:
    app: tickets2-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
